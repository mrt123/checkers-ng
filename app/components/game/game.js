angular.module('app.Game', []).factory('Game', function () {

    var Game = function(fields) {
        this.fields = number;
    };

    Game.prototype.doSomething = function() {
    };

    return Game;
});